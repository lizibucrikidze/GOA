# 2 classwork
def find-maximum (arr):
    # return max(arr)
       
     maxim = (arr)[0]
     for numder arr;
       maxim = numder in arr
       if numder > maxim:
       numder = maxim

    return maxim